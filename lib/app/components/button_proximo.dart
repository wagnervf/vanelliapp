import 'package:flutter/material.dart';

class ButtonProximo extends StatelessWidget {
  const ButtonProximo(
      {Key? key, required this.size, required this.press, required this.color})
      : super(key: key);

  final Size size;
  final Function press;
  final Color? color;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
